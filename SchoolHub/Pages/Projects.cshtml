@page
@model SchoolHub.Pages.ProjectsModel
@{
    ViewData["Title"] = "Проекты";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">Все проекты</h1>
        <span class="badge bg-dark fs-6">Всего проектов: @Model.TotalProjectsCount</span>
    </div>

    @if (!string.IsNullOrEmpty(Model.Message))
    {
        <div class="alert alert-warning">
            @Model.Message
        </div>
    }

    <div class="card shadow mb-4">
        <div class="card-body">
            <h3 class="mb-3">Добавить проект</h3>

            <form method="post" asp-page-handler="Add">
                <div class="mb-3">
                    <label class="form-label">Название проекта</label>
                    <input asp-for="Title" class="form-control" />
                </div>

                <div class="mb-3">
                    <label class="form-label">Описание</label>
                    <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                </div>

                <div class="mb-3">
                    <label class="form-label">Категория</label>
                    <select asp-for="Category" class="form-select">
                        <option value="">Выберите категорию</option>
                        @foreach (var category in Model.Categories)
                        {
                            <option value="@category">@category</option>
                        }
                    </select>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-success">
                        Добавить проект
                    </button>

                    <button type="reset" class="btn btn-secondary">
                        Сбросить
                    </button>
                </div>
            </form>
        </div>
    </div>

    <h3 class="mb-3">Список проектов</h3>

    @if (Model.Projects.Count == 0)
    {
        <div class="alert alert-info">
            Пока проектов нет.
        </div>
    }
    else
    {
        <div class="row">
            @foreach (var project in Model.Projects)
            {
                <div class="col-md-6 mb-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body">
                            <h4 class="card-title">@project.Title</h4>

                            <p class="mb-2">
                                <span class="badge bg-primary">@project.Category</span>
                            </p>

                            <p class="card-text">@project.Description</p>
                        </div>

                        <div class="card-footer">
                            <small class="text-muted">
                                Автор: @project.Author?.Name
                            </small>
                            <small class="d-block text-muted">
                                Дата создания: @project.CreatedAt.ToString("dd.MM.yyyy HH:mm")
                            </small>

                            <a asp-page="/ProjectDetails"
                               asp-route-id="@project.Id"
                               class="btn btn-outline-primary btn-sm">
                                Открыть
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>